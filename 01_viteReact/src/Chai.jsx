// always name your component file with .jsx if its returning html. 
// component file is file where you define diff components (fn).
// always name your file as well as fn with first letter Capital.

function Chai(){
    return (
        <>
        <h3>chai</h3>
        </>
    )
}

export default Chai // this statement exports this 'Chai' fn whenever import is called on this file.